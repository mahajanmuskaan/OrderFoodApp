/**
 * config.js in a project to store configuration variables or settings that can be accessed 
 * throughout the application.
 * 
 * Constants defined here can be export(Good Practice- use named export) and,
 * named import in the files wherever they are needed. 
 * 
 * By using a config.js file, you can easily modify these settings 
 * without having to search through your codebase to find all the places where the values are used.
 */